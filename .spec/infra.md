# The Reality

## Runtime Constraints
Static web hosting (e.g., GitHub Pages, Vercel, Netlify).

## Deployment Rules
Deploy by serving the `index.html` file and `assets/` directory at the root.

## Observability
None currently defined. Relies on standard static hosting analytics.